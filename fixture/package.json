{
  "name": "test",
  "type": "module",
  "engines": {
    "node": ">=13.2.0"
  },
  "scripts": {
    "clean": "rm -rf ./.loader_cache/* /tmp/loader-cache/jspm",
    "start": "npx serve",
    "test": "node --loader ../index.js ./test.js"
  }
}
